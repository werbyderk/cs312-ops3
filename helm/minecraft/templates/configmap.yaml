apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "minecraft.fullname" . }}-config
  namespace: {{ .Values.namespace }}
data:
  server.properties: |
    motd={{ .Values.minecraft.motd }}
    max-players={{ .Values.minecraft.maxPlayers }}
    difficulty={{ .Values.minecraft.difficulty }}
