# ansible/playbook.yml
- name: Deploy Minecraft server
  hosts: all
  become: true
  vars_files:
    - vars/main.yml

  pre_tasks:
    - name: Gather facts (needed for ansible_facts)
      ansible.builtin.setup:

  tasks:
    - import_tasks: tasks/main.yml
